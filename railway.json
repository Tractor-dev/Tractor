{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run postinstall && python3 -m venv who_venv && source who_venv/bin/activate && pip install -r WhoDesigned/requirements.txt"
  },
  "deploy": {
    "startCommand": "source who_venv/bin/activate && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
